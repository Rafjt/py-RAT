## Pour lancer le projet:

1. Ouvrir deux terminaux de commandes
2. Naviguer à la racine du projet dans les deux: py-RAT>
3. Dans le premier, lancer la commande `poetry run python run_server.py`
4. Dans le second, lancer la commande `poetry run python run_client.py`
